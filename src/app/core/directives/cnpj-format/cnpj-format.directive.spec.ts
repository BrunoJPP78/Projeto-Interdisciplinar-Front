import { CnpjFormatDirective } from "./cnpj-format.directive";
describe('CnpjFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new CnpjFormatDirective();
    expect(directive).toBeTruthy();
  });
});
